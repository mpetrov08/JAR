@model JobOffersFilterModel;

@{
    ViewBag.Title = "Search Job Offers";
}

<h2 class="text-center">@ViewBag.Title</h2>
<hr />


<form method="get">
    <div class="row">
        <div class="form-group col-md-3">
            <label asp-for="SearchTerm"></label>
            <input asp-for="SearchTerm" class="form-control rounded" placeholder="Search" aria-label="Search" aria-describedby="search-addon" />
        </div>

        <div class="form-group col-md-3">
            <label asp-for="Category"></label>
            <select asp-for="Category" class="form-control rounded">
                <option value="">All</option>
                @foreach (var category in Model.Categories)
                {
                    <option value="@category">@category</option>
                }
            </select>
        </div>

        <div class="form-group col-md-3">
            <label asp-for="JobType"></label>
            <select asp-for="JobType" class="form-control rounded">
                <option value="">All</option>
                @foreach (var jobType in Model.JobTypes)
                {
                    <option value="@jobType">@jobType</option>
                }
            </select>
        </div>

        <div class="form-group col-md-3">
            <label asp-for="Sorting"></label>
            <select asp-for="Sorting" class="form-control rounded">
                <option value="0">Newest</option>
                <option value="1">Highest Salary</option>
                <option value="2">Lowest Salary</option>
            </select>
        </div>

        <div class="col-md-12">
            <div class="form-group mt-4 d-flex justify-content-center">
                <input type="submit" value="Search" class="btn btn-outline-primary" />
            </div>       
        </div>
    </div> 
</form>


@{
    var previousPage = Model.CurrentPage - 1;
    if (previousPage < 1)
    {
        previousPage = 1;
    }

    var maxPage = Math.Ceiling((double)Model.TotalJobOffersCount /
        Model.JobOffersPerPage);
}

<div class="row mb-5">
    <div class="col-md-6 d-grid gap-2 d-md-flex justify-content-md-start">
        <a class="btn btn-primary @(Model.CurrentPage == 1 ? "disabled" : string.Empty)"
           asp-controller="JobOffer"
           asp-action="All"
           asp-route-currentPage="@previousPage"
           asp-route-category="@Model.Category"
           asp-route-searchTerm="@Model.SearchTerm"
           asp-route-sorting="@((int)Model.Sorting)"><<</a>
    </div>

    @{
        var shouldButtonBeDisabled = Model.CurrentPage == maxPage ||
        !Model.JobOffers.Any();
    }

    <div class="col-md-6 d-grid gap-2 d-md-flex justify-content-md-end">
        <a class="btn btn-primary
           @(shouldButtonBeDisabled ? "disabled" : string.Empty)"
           asp-controller="JobOffer"
           asp-action="All"
           asp-route-currentPage="@(Model.CurrentPage + 1)"
           asp-route-category="@Model.Category"
           asp-route-searchTerm="@Model.SearchTerm"
           asp-route-sorting="@((int)Model.Sorting)">>></a>
    </div>
</div>

@foreach (var jobOffer in Model.JobOffers)
{
    <div class="card">
        <h5 class="card-header">@jobOffer.Title</h5>

        <div class="card-body">
            <h5 class="card-title">@jobOffer.Address</h5>
            <h6>@jobOffer.Salary$</h6>
            <p class="card-text">
                @jobOffer.RequiredSkills

                @if (jobOffer.RequiredLanguage != string.Empty)
                {
                    @jobOffer.RequiredLanguage
                }

                @if (jobOffer.RequiredDegree != string.Empty)
                {
                    @jobOffer.RequiredDegree
                }

                @if (jobOffer.RequiredExperience != string.Empty)
                {
                    @jobOffer.RequiredDegree
                }              
            </p>

            <p class="card-text">@jobOffer.CreatedOn</p>

            <div class="form-group col-md-3">
                <a class="btn btn-primary" asp-area="" asp-controller="JobOffer" asp-action="Details" asp-route-id="@jobOffer.Id">Details</a>
                <a class="btn btn-primary" asp-area="" asp-controller="JobOffer" asp-action="Edit" asp-route-id="@jobOffer.Id">Edit</a>
                <a class="btn btn-primary" asp-area="" asp-controller="JobOffer" asp-action="Delete" asp-route-id="@jobOffer.Id">Delete</a>
            </div>
        </div>
    </div>

    <br />
}

@section Scripts {
    <partial name="_ValidationScriptsPartial"></partial>
}