@model JobOfferDetailsViewModel

@{
    ViewBag.Title = "Delete Job Offer";
}

<h3 class="text-center text-primary mb-4">@ViewBag.Title</h3>
<hr />

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-lg border-0 rounded">
                <div class="card-body">
                    <h4 class="text-center mb-4 text-danger">
                        <i class="fas fa-trash-alt"></i> Are you sure you want to delete this job offer?
                    </h4>

                    <form asp-action="Delete">
                        <div class="mb-3">
                            <label asp-for="Title" class="form-label"><i class="fas fa-briefcase"></i> Job Title</label>
                            <input asp-for="Title" class="form-control" disabled="disabled" />
                        </div>

                        <div class="mb-3">
                            <label class="form-label"><i class="fas fa-file-alt"></i> Description</label>
                            <div class="form-text bg-light p-3 rounded" style="white-space: pre-wrap;">
                                @Html.Raw(Model.Description.RemoveHtmlXss())
                            </div>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Address" class="form-label"><i class="fas fa-map-marker-alt"></i> Location</label>
                            <input asp-for="Address" class="form-control" disabled="disabled" />
                        </div>

                        <div class="d-grid gap-2 mt-4">
                            <input type="submit" value="Delete Job Offer" class="btn btn-danger btn-lg">

                            <a asp-area="" asp-controller="JobOffer" asp-action="All" class="btn-primary btn btn-lg">
                                <i class="fas fa-times-circle"></i> Cancel
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Styles {
    <link rel="stylesheet" href="~/css/jobOffer/deleteJobOffer.css" />
}